require 'spec_helper'

RSpec.describe "Command line files" do
  # it "blissrunner prompts for command" do
  #   @blissrunner = BlissRunner.new
  #   @blissrunner.should_receive(:puts).with('Which command would you like to run? ((C)ollector, (L)int or (S)tats or (Q)uit)')
  #   @blissrunner.choose_command
  # end
  #
  # it "run collector over an empty director" do
  #   Dir.mktmpdir do |git_dir|
  #     collector = Collector.new
  #     collector.git_init(git_dir)
  #
  #   end
  # end

end
